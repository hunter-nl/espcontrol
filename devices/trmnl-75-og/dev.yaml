# =============================================================================
# DEV CONFIG - Local development entry point (TRMNL 7.5 OG)
# =============================================================================

substitutions:
  name: "trmnl-75-og-dev"
  friendly_name: "TRMNL 7.5 OG Dev"
  espcontrol_component_url: "../.."
  espcontrol_component_ref: "HEAD"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "trmnl-75-og-dev-fallback"
    password: "changeme123"

packages:
  espcontrol: !include packages.yaml
